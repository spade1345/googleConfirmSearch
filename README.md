# Google Search Confirmation Page

This project is a simple HTML page that confirms a user's intent to search Google for a specific query. The query is passed as a URL parameter and displayed on the page.
